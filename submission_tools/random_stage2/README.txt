Random baseline requested by the user because private source FPS is unknown.
No checkpoint, FPS, image decoding or GPU is needed. Outputs are deterministic
pseudorandom per video ID, with actual frame indices and entry <= collision.
These are placeholders, not learned accident predictions.
The trained DINOv3-S and temporal head remain in submission_tools/learned_stage2/.
