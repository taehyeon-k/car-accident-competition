Learned FPS-independent Stage 2: tuned no-anchor DINOv3-S plus a small temporal head.
128 normalized clip positions; no FPS or duration metadata required.
Head refit on all 251 labeled clips. See training_report.json for held-out selection results.
Checkpoint/config sampling settings must match. No random fallback is used.
