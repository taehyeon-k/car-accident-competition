# BDD100K only (static; BDD has no temporal pairs).
base: ablation_base.yaml
output_dir: /workspace/outputs/geometry_pretrain/runs/abl_bdd_only
tasks: [road, depth, objects, contact, camera]
data: {static_weights: {bdd100k: 1.0, tusimple: 0.0, accident: 0.0, baton: 0.0}, pair_batch_size: 0}
